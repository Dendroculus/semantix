"""FastAPI route modules for public Semantix endpoints.

Routes validate HTTP input, invoke domain services, and return typed schemas.
They should remain thin and must not absorb cache or provider implementation logic.
"""
