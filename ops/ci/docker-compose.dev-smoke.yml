services:
  backend:
    environment:
      CACHE_BACKEND: ${CACHE_BACKEND}
      DATABASE_URL: ${DATABASE_URL:-}
      EMBEDDING_PROVIDER: ${EMBEDDING_PROVIDER}
      GENERATION_PROVIDER: ${GENERATION_PROVIDER}
      MOCK_EMBEDDING_DIMENSIONS: ${MOCK_EMBEDDING_DIMENSIONS:-384}
